#include <iostream>
#include <string.h>

using namespace std;

int main() {
  char a[101] = "";
  cin >> a;
  int n = strlen(a);
  for(int i = 0; i < n / 2; i++){
    if(a[i] == a[n - i - 1])
      continue;
    else{
      cout << 0;
      return 0;
    }
  }
 
  cout << 1;
}
