#include <iostream>
using namespace std;

int main() {
    int arr[10000];
    int a, c;
    scanf("%d %d", &a, &c);

    for(int i = 0; i<a; i++){
        scanf("%d", &arr[i]);
    }

    for(int i = 0; i<a; i++){
            if(arr[i]<c){
                cout << arr[i] << " ";
            }
        }
}
