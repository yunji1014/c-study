#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    if ((c - a) % (a - b))
        cout << (c - a) / (a - b) + 2;
    else
        cout << (c - a) / (a - b) + 1;
}
//부동소수점.. 유의 
