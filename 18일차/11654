#include <iostream>
using namespace std;

int main()
{
    char a;
	cin >> a;
    
	if (a >= 'a' && a <= 'z') {
		cout << (int(a));
	}
    if (a>='A'&& a<='Z'){
        cout << (int(a));
    }
    if (a>='0'&& a<='9'){
        cout << (int(a));
    }
	return 0;
}
