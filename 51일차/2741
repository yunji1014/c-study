#include <iostream>
using namespace std;

int main() {
    cin.tie(NULL);
    sync_with_stdio(false);
    
    int a;
    cin >> a;
    for(int i=1; i<=a; i++){
        cout << i << "\n";
    }
    return 0;
} 
